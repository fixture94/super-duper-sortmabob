#def main: 
#openPDf(filePath)
#boundWindow(topLeft, topRight, bottomLeft, bottomRight)
#scanPdf()
#split a pdf into smaller PDFs based on idexes.
#I can get the indexes by scanning the PDF with a bounded box, scrolling through PDF (need # of pages). 

#!python3 
#mouseNow.py displays the mouse cursor's current position

import pyautogui
print('Press Ctrl-C to quit.')
pyautogui.displayMousePosition()
    
        
